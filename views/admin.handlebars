<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css"/>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <title>Admin</title>
</head>
<body>
    <h1>Admin</h1>
    <a href="/">Go back to Home Page</a>
    <i class='bx bx-edit-alt'></i>

    <div class="content">
        <div class="banner"></div>
        <button onclick="openAdminPanel()">add service</button>
        <div class="services_container">
            {{#each services}}
            <div class="service" onclick="openServicePanelAdmin('{{service_name}}', '{{service_description}}', '{{this.service_img}}')">
                <img src="img/sample.jpg" alt="img/sample.jpg" class="service_image">
                <p class="service_name">{{service_name}}</p>
                <p class="service_description">{{service_description}}</p>
                <i class='bx bx-edit-alt'></i>
            </div>
            {{/each}}
        </div>
    </div>

    <div id="servicePanel" class="panel">
        <div class="panel_content">
            <div class="close_btn">
                <i class='bx bx-x-circle' onclick="closePanel()"></i>
            </div>
            <div class="pannel_service_img"> 
                <img id="panelServiceImage">
            </div>
            <h3 id="panelServiceName"></h3>
            <p id="panelServiceDescription"></p>
            <button class="option_schedule" onclick="optionSchedule()">Schedule</button>
            <button class="option_print" onclick="optionPrint()">Print</button>
        </div>
    </div>

    <div id="adminPanel" class="panel">
        <div class="panel_content">
            <div class="close_btn">
                <i class='bx bx-x-circle' onclick="closeAdminPanel()"></i>
            </div>
            <h3>Add New Service</h3>
            <form id="addServiceForm" onsubmit="addService(event)">
                <label for="serviceName">Name:</label>
                <input type="text" id="serviceName" name="serviceName" required>
                <label for="serviceDescription">Description:</label>
                <textarea id="serviceDescription" name="serviceDescription" required></textarea>
                <label for="servicePrice">Price:</label>
                <input type="number" id="servicePrice" name="servicePrice" required>
                <button type="submit">Add Service</button>
            </form>
        </div>
    </div>

    <script src="js/script.js"></script>
</body>
</html>