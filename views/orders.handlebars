<nav>
    <h1 class="title">Orders</h1>
    <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/admin">Admin</a></li>
    </ul>
</nav>
<body>
    <br>
    {{#each orders}}
    <div class="orders">
        <h3>#{{order_id}}</h3>
        <img src="{{order_img}}" alt="" class="order_img">
        <p>{{order_name}}</p>
        <p>{{order_date}}</p>
        <p>state</p>
    </div>
    {{/each}}
</body>
